var radius = 5; // an integer
var pi = 3.142; // a floating point number

// Methematical operations
const sum = radius + pi;
const difference = radius - pi;
const division = radius / pi;
const product = radius * pi;
const remainder = radius % pi;


// QUANT CODE